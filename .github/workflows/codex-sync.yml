name: Codex sync

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  codex-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Codex sync
        run: python3 scripts/check_codex_sync.py --check
