# CONTRIBUTING

このハーネスは **「できました」を終了コードで潰す**ためのものです。
だから貢献の作法も、宣言でなく exit code で決まります。

## 送る前に

```sh
./verify.sh
```

**exit 0 でなければ受け取れません。** これはレビュアの好みではなく、
配布物が宣伝どおり動くことの唯一の証明だからです。

## 3 つの原則（`core/docs/principles.md` が正本）

| | |
|---|---|
| **P1 自己申告禁止** | 「直しました」でなく、**落ちるテストを 1 本足してから**直す |
| **P2 停止は決定論で判定する** | 「たぶん大丈夫」で止めない。上限と終了条件を書く |
| **P3 承認境界は 可逆性 × 外部到達 × 金銭/法的 で決める** | 「重要そう」で決めない |

## ゲートを足すときは、同じ PR に self-test を書く

**block 側・pass 側の両方**に加えて、**そのゲートが案内する直し方で実際に通るケース**を必ず入れてください。

> 実例: 本リポのゲートが「`[ "$N" -eq 0 ]` と書け」と案内しながら、
> 引用符を許しておらず**自分の推奨手順を落として**いました。3 つ目のケースが無ければ出荷されていました。

## 過検出は「無いこと」より悪い

**使われなくなったゲートは 0 件しか止めません。** 語彙を広げる前に、
実際のコードに当てて何件出るかを測ってください。逃げ道を作るときは**理由を必須**にしてください
（理由の無い逃げ道は、静かに恒久化します）。

## 空出力を「0 件」と書かない

取得に失敗したのか、本当に 0 件だったのかを**出力で区別**してください。
失敗は `❓ 不明` として扱い、走査 0 件は正常系にしないでください。件数は**必ず分母つき**で出します。

## PR に書くこと

- **なぜ**（何が壊れていたか。症状でなく原因）
- **実測**（コマンドと exit code。「動きました」は実測ではありません）
- ゲートを足したなら、**変異させると落ちること**を確かめた記録
